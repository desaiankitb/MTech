	#include<stdio.h>
	#include<conio.h>

	int main()
	{
		char chArFile[80];
		FILE * fpCipher;

		printf("Enter a file name to decrypt(include path)::");
		gets(chArFile);

		strcat("d:\\ANKIT\\CNS\\PRGS\\",chArFile);
		fpCipher = fopen(chArFile,

		return 0;
	}